<Controls:MetroWindow x:Class="AzureLauncher.MainWindow"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="pack://application:,,,/MaterialDesignThemes.Wpf;component/Resources/Roboto/#Roboto"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AzureLauncher"
        mc:Ignorable="d"
        Title="AzureLauncher" Height="250" Width="450" ResizeMode="NoResize">
    <Controls:MetroWindow.RightWindowCommands>
        <Controls:WindowCommands>
            <Button x:Name="ConfigBtn" Content="Config" Click="ConfigBtn_Click"></Button>
            <Button x:Name="LoginBtn" Content="Log in" Click="LoginBtn_Click"></Button>
        </Controls:WindowCommands>
    </Controls:MetroWindow.RightWindowCommands>
    <Grid>
        <ContentControl x:Name="Container" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"></ContentControl>
    </Grid>
</Controls:MetroWindow>
